import { Page } from "../components/Page";

export const User = () => {
    return (
        <div className="gradient">
            <Page Title="User">
                <h1>User</h1>
            </Page>
        </div>
    );
}